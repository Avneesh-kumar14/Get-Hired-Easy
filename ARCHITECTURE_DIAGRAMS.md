# 🔄 Get-Hired-Easy: Request/Response Flow Diagrams

## 🏗️ Overall Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    BROWSER (http://localhost:5174)              │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │              React App (Vite)                             │  │
│  │  ┌──────────────────────────────────────────────────────┐ │  │
│  │  │ Pages:                                               │ │  │
│  │  │ • Home, Login, Signup                               │ │  │
│  │  │ • Browse Jobs, Job Details                          │ │  │
│  │  │ • Admin: Companies, Jobs, Applicants               │ │  │
│  │  │ • Status, Profile, Settings                         │ │  │
│  │  └──────────────────────────────────────────────────────┘ │  │
│  │  ┌──────────────────────────────────────────────────────┐ │  │
│  │  │ Redux Store:                                         │ │  │
│  │  │ • auth (user, loading)                              │ │  │
│  │  │ • jobs, companies, applications                     │ │  │
│  │  └──────────────────────────────────────────────────────┘ │  │
│  └───────────────────────────────────────────────────────────┘  │
└──────────────────┬──────────────────────────────────────────────┘
                   │
         ┌─────────┴─────────┐
         │  CORS Configured  │
         │  Allow Port 5174  │
         └─────────┬─────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────────────┐
│           Node.js Backend (http://localhost:8200)               │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │           Express.js Application                          │  │
│  │  ┌──────────────────────────────────────────────────────┐ │  │
│  │  │ Routes:                                              │ │  │
│  │  │ • /api/v1/user (signup, login, profile)            │ │  │
│  │  │ • /api/v1/job (post, get, filter)                 │ │  │
│  │  │ • /api/v1/company (register, update)              │ │  │
│  │  │ • /api/v1/application (apply, status)             │ │  │
│  │  │ • /api/v1/report/issue (report)                   │ │  │
│  │  └──────────────────────────────────────────────────────┘ │  │
│  │  ┌──────────────────────────────────────────────────────┐ │  │
│  │  │ Middleware:                                          │ │  │
│  │  │ • Auth (JWT verification)                          │ │  │
│  │  │ • Multer (file upload)                             │ │  │
│  │  │ • CORS, Cookie Parser, Body Parser                │ │  │
│  │  └──────────────────────────────────────────────────────┘ │  │
│  └───────────────────────────────────────────────────────────┘  │
└──────────────────┬──────────────────────────────────────────────┘
                   │
        ┌──────────┴──────────┐
        │                     │
        ▼                     ▼
┌──────────────────┐   ┌──────────────────────────┐
│   MongoDB Atlas  │   │   Cloudinary            │
│                  │   │   (Image Storage)       │
│ • Users          │   │                        │
│ • Jobs           │   │ Upload/Retrieve Images │
│ • Companies      │   │                        │
│ • Applications   │   │                        │
│ • Issues         │   │                        │
└──────────────────┘   └──────────────────────────┘
```

---

## 📝 Signup Flow (Complete)

```
┌─────────────────────┐
│  User Signup Page   │
│  (localhost:5174)   │
└──────────┬──────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  1. User fills form:                 │
│  • fullName                          │
│  • email                             │
│  • phoneNumber                       │
│  • password                          │
│  • role (student/recruiter)          │
│  • file (optional)                   │
└──────────┬───────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  2. Form Validation                  │
│  • Check required fields             │
│  • Validate email format             │
│  • Password strength check           │
│  • File type/size (if uploaded)      │
└──────────┬───────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  3. Create FormData                  │
│  • Add all fields                    │
│  • Skip null file                    │
│  • Don't set Content-Type header     │
└──────────┬───────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  4. Send POST Request                │
│  POST /api/v1/user/signup            │
│  Host: localhost:8200                │
│  Body: FormData                      │
│  withCredentials: true               │
└──────────┬───────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  5. Backend singleUpload Middleware  │
│  • Extract file from request         │
│  • Store in memory buffer            │
└──────────┬───────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  6. Signup Controller                │
│  • Validate all fields required      │
│  • Check if email exists             │
│  • If file: upload to Cloudinary    │
│  • Hash password with Bcrypt        │
│  • Create user in MongoDB            │
│  • Generate JWT token                │
│  • Set HTTPOnly cookie               │
└──────────┬───────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  7. Send Response                    │
│  Status: 201 Created                 │
│  {                                   │
│    success: true,                    │
│    message: "User created",          │
│    user: {                           │
│      _id, fullName, email,           │
│      phoneNumber, role,              │
│      profile: { profilePicture }     │
│    }                                 │
│  }                                   │
└──────────┬───────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  8. Frontend Handles Response        │
│  • Store user in Redux               │
│  • Set cookie from response          │
│  • Show success toast                │
│  • Navigate to home/admin            │
└──────────────────────────────────────┘
```

---

## 🔐 Login Flow

```
┌──────────────────┐
│  Login Page      │
│  localhost:5174  │
└────────┬─────────┘
         │
         ▼
┌────────────────────────────────┐
│ User enters:                   │
│ • email                        │
│ • password                     │
│ • role (student/recruiter)     │
└────────┬───────────────────────┘
         │
         ▼
┌────────────────────────────────┐
│ POST /api/v1/user/login        │
│ Host: localhost:8200           │
└────────┬───────────────────────┘
         │
         ▼
┌────────────────────────────────┐
│ Backend Login Controller       │
│ 1. Find user by email         │
│ 2. Compare password hash      │
│ 3. Generate JWT token         │
│ 4. Set cookie                 │
│ 5. Return user data           │
└────────┬───────────────────────┘
         │
         ▼
┌────────────────────────────────┐
│ Response: User + JWT in Cookie │
└────────┬───────────────────────┘
         │
         ▼
┌────────────────────────────────┐
│ Frontend:                      │
│ • Redux store user            │
│ • Auto-login for session      │
│ • Redirect to dashboard       │
└────────────────────────────────┘
```

---

## 💼 Job Application Flow

```
┌─────────────────────────────────┐
│  Browse Jobs Page               │
│  GET /api/v1/job/get            │
│  ↓                              │
│  Display list of all jobs       │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  Click Job Card                 │
│  → Navigate to Job Details      │
│  → GET /api/v1/job/get/:id      │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  Job Details Page               │
│  • Job title, description       │
│  • Company info                 │
│  • Salary, location             │
│  • "Apply" button               │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  Click "Apply"                  │
│  POST /api/v1/application/apply │
│  Body: { jobId }                │
│  Headers: { Auth: JWT token }   │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  Backend Middleware:            │
│  isAuthenticated (verify JWT)   │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  Apply Controller:              │
│  • Check if already applied     │
│  • Create application record    │
│  • Send email to recruiter      │
│  • Return application object    │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  Frontend Response:             │
│  • Show success toast           │
│  • Enable "Withdraw" button     │
│  • Update application status    │
└────────────────────────────────┘
```

---

## 👔 Recruiter: Post Job Flow

```
┌──────────────────────┐
│  Admin Dashboard     │
│  Recruiter Only      │
└────────┬─────────────┘
         │
         ▼
┌──────────────────────────────────┐
│  Click "Post Job"                │
│  → Job Form Page                 │
└────────┬─────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│  Fill Job Form:                  │
│  • title                         │
│  • description                   │
│  • requirements                  │
│  • salary (min-max)              │
│  • location                      │
│  • experience level              │
│  • positions (count)             │
│  • companyId                     │
└────────┬─────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│  POST /api/v1/job/post           │
│  Auth: JWT token required        │
│  Body: job details               │
└────────┬─────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│  Backend:                        │
│  • Verify recruiter role        │
│  • Validate job data            │
│  • Create job in MongoDB        │
│  • Return created job           │
└────────┬─────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│  Frontend:                       │
│  • Show success message          │
│  • Redirect to job list          │
│  • New job visible in browse     │
└──────────────────────────────────┘
```

---

## 📊 Data Models

### User Model
```javascript
{
  _id: ObjectId,
  fullName: String,
  email: String (unique),
  phoneNumber: String (unique),
  password: String (hashed),
  role: "student" | "recruiter",
  profile: {
    profilePicture: URL,
    bio: String,
    skills: [String],
    resume: URL
  },
  gender: "male" | "female" | "other",
  social: {
    linkedin: URL,
    github: URL
  },
  otp: String,
  otpExpires: Date,
  createdAt: Date,
  updatedAt: Date
}
```

### Job Model
```javascript
{
  _id: ObjectId,
  title: String,
  description: String,
  requirements: [String],
  salary: { min: Number, max: Number },
  location: String,
  jobType: "full-time" | "part-time",
  experience: String,
  positions: Number,
  company: ObjectId (ref: Company),
  createdBy: ObjectId (ref: User),
  applications: [ObjectId],
  createdAt: Date,
  updatedAt: Date
}
```

### Application Model
```javascript
{
  _id: ObjectId,
  job: ObjectId (ref: Job),
  applicant: ObjectId (ref: User),
  status: "pending" | "accepted" | "rejected",
  appliedAt: Date,
  updatedAt: Date
}
```

### Company Model
```javascript
{
  _id: ObjectId,
  name: String,
  description: String,
  website: URL,
  location: String,
  logo: URL,
  userId: ObjectId (ref: User),
  employees: [ObjectId],
  createdAt: Date,
  updatedAt: Date
}
```

---

## 🔄 Redux State Structure

```javascript
{
  auth: {
    user: {
      _id, fullName, email, role,
      profile: { profilePicture }
    },
    loading: Boolean,
    error: String
  },
  
  job: {
    allJobs: [Job],
    adminJobs: [Job],
    singleJob: Job,
    loading: Boolean,
    searchText: String
  },
  
  company: {
    singleCompany: Company,
    companies: [Company],
    searchCompanyByText: String,
    loading: Boolean
  },
  
  application: {
    applicants: [Application],
    applied: [Application],
    loading: Boolean
  }
}
```

---

## 🛡️ CORS & Security Flow

```
Browser (5174)
    │
    ├─→ Preflight Request (OPTIONS)
    │   Headers:
    │   - Origin: http://localhost:5174
    │   - Access-Control-Request-Method: POST
    │
Backend (8200)
    │
    └─→ Check CORS Middleware
        └─→ Allow if Origin matches CLIENT_URL
            └─→ Allow if method in ["GET", "POST", "PUT", "PATCH", "DELETE"]
                └─→ credentials: true ✓
                    └─→ Send CORS response headers

Browser receives:
    - Access-Control-Allow-Origin: http://localhost:5174
    - Access-Control-Allow-Credentials: true
    - Access-Control-Allow-Methods: GET, POST, PUT, PATCH, DELETE

Then actual request proceeds with cookie forwarding ✓
```

---

## 📧 Email Service Flow

```
Backend Event (signup, application, etc)
    │
    └─→ Nodemailer Config
        ├─ Service: Gmail
        ├─ User: EMAIL_USER
        ├─ Pass: PASS_USER
        └─ From: EMAIL_USER
    
    └─→ Email Template (HTML)
        ├─ OTP Template
        ├─ Application Status Template
        ├─ Job Withdrawal Template
        └─ Issue Report Template
    
    └─→ Send Email
        ├─ To: User email
        ├─ Subject: Context-based
        └─ Body: HTML template
    
Result: User receives email notification
```

---

## 🔑 Authentication Flow

```
1. User Signup
   └─→ Hash password (Bcrypt)
   └─→ Store in MongoDB
   └─→ Generate JWT: jwt.sign({ userId }, SECRET_KEY, { expiresIn: "1d" })

2. User Login
   └─→ Find user by email
   └─→ Compare password (Bcrypt compare)
   └─→ Generate JWT token
   └─→ Set HTTP-Only cookie: { jwtToken, httpOnly, secure, sameSite: "none" }

3. Protected Routes
   └─→ Extract JWT from cookie
   └─→ Verify: jwt.verify(token, SECRET_KEY)
   └─→ If valid: attach userId to request
   └─→ If invalid: send 401 Unauthorized

4. Logout
   └─→ Clear cookie
   └─→ Frontend clears Redux store
```

---

**This diagram shows the complete flow of data through the Get-Hired-Easy platform!**
